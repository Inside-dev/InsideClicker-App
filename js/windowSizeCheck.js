let windowResizeCheck = window.onresize(() => {
    console.log(window.innerWidth);
});

let windowLoadCheck = () => {
    console.log(window.innerWidth);
};

export default windowResizeCheck;
export default windowLoadCheck;
